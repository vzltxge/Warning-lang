- Make errors more descriptive. You could make a custom class for handling errors.
- Make for loops step optional and default to 1.


- Make sure every value uses the smallest space it can use by default if it is not assigned to a variable.